var should = require('should')
  , stochasm = require('package')

describe('+ stochasm', function() {
  describe('> when no parameters', function() {
    it('should create a function that generates values between 0.0 and 1.0', function() {
      
    })
  })
})